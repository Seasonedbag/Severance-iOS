name: Build iOS IPA

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Build Severance iOS App
      run: |
        echo "⚒ Building Severance iOS app on macOS runner"
        ls -R

    - name: Upload output
      uses: actions/upload-artifact@v4
      with:
        name: severance-ipa
        path: .

